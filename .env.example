# OpenCode API Keys
# Rename this file to .env and add your API keys

# OpenCode API Key (if required)
# OPENCODE_API_KEY=your_opencode_api_key_here

# Anthropic API Key (for Claude models)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (for GPT models)
# OPENAI_API_KEY=your_openai_api_key_here

# OpenCode Web Server Configuration
# Authentication
# OPENCODE_SERVER_USERNAME=opencode
# OPENCODE_SERVER_PASSWORD=your_secure_password_here

# Network Configuration
# Port for OpenCode web server inside container (default: random, can be fixed)
# OPENCODE_SERVER_PORT=4096
# Host port mapping on the host machine (maps to OPENCODE_SERVER_PORT)
# OPENCODE_SERVER_HOST_PORT=4096
# Hostname to bind to (0.0.0.0 for all interfaces, 127.0.0.1 for localhost only)
# OPENCODE_SERVER_HOSTNAME=0.0.0.0

# mDNS Discovery
# Enable mDNS discovery on local network
# OPENCODE_SERVER_MDNS=false
# Custom mDNS domain (default: opencode.local)
# OPENCODE_SERVER_MDNS_DOMAIN=opencode.local

# CORS Configuration
# Comma-separated list of allowed CORS origins
# OPENCODE_SERVER_CORS=https://example.com,http://localhost:3000

# Docker Image Configuration
# Docker image to use (default: ghcr.io/haytham-ai-assistant/opencode-docker-web:latest)
# DOCKER_IMAGE=ghcr.io/haytham-ai-assistant/opencode-docker-web:latest
